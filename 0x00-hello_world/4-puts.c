#include<stdio.h>
/**
 * We are about to print a puzzle
 *
 * It will just print so the return is 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

